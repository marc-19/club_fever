<div class="profile-section">
  <div class="profile-card">
  <h2>Your Profile</h2>
    <% if @user.avatar.attached? %>
      <img src="<%= url_for(@user.avatar) %>" alt="<%= @user.first_name %>'s Avatar" class="profile-avatar" />
    <% else %>
      <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQ2BbQk-mYW4VgUkiL_vuEOWXtVBFyRNPcvCQ&s" alt="Default Avatar" class="profile-avatar" />
    <% end %>

    <h3 class="profile-username"><%= @user.first_name %></h3>

    <p class="profile-details">10 quinielas played</p>

    <button class="btn-yellow">Edit Profile</button>
  </div>
</div>

<div class = club-edit-card>
  <h2>Clubs you follow</h2>
    <div class="card-footer">
      <p> Display clubs logos followed here</p>
      <br>
      <p> Search for new club to follow:</p>
      <div class="search-bar-clubs">
        <%= simple_form_for :search, url: clubs_path, method: :get do |f| %>
          <%= f.input :search, placeholder: "Search for your club...", label: false %>
        <% end %>
      </div>
    </div>
</div>

<%# copy paste show.html.erb active-quiniela-show but dislpaying the quinielas of
the followed clubs %>

<div class="active-quinielas-show">
  <h2 class="h2-show">Playable Quinielas</h2>
  <p>You can see the quinielas that you can play of your clubs here</p>
</div>

<div class="past-quinielas-show">
  <h2 class="h2-show">Played Quinielas</h2>
  </div>
<% if @past_predictions.any? %>
  <% @past_predictions.each do |prediction| %>
  <div class="past-quinielas-show">
    <div class="quiniela-card-show">
    <div class='quiniela-header-show'>
      <div class="quiniela-logo">
            <%# <%= cl_image_tag @club.logo.key if @club.logo.attached %>
          </div>
      <h3 class="h3-show"><%= prediction.quiniela.title %></h3>
      </div>
      <p><strong>Submitted:</strong> <%= prediction.created_at.strftime("%d %B %Y") %></p>
      <p><strong>Your Prediction:</strong> <%= prediction.result.join(", ") %></p>
      <button class="btn-yellow">Results</button>
    </div>
    </div>
  <% end %>
<% else %>
  <p>No past predictions recorded.</p>
<% end %>
</div>

<%# I need to do this predictions work to get access to the results quiniela and also displaying the club logo%>
